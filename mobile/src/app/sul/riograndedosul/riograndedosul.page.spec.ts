import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RiograndedosulPage } from './riograndedosul.page';

describe('RiograndedosulPage', () => {
  let component: RiograndedosulPage;
  let fixture: ComponentFixture<RiograndedosulPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RiograndedosulPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
